/**
 * Hook for camera capture and library selection.
 * Will delegate to services/imagePicker once implemented.
 */

export function useImagePicker() {
  // Placeholder — implementation will be added later.
  return {
    captureFromCamera: async () => {},
    pickFromLibrary: async () => {},
  };
}
